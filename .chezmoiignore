README.md
RECORD.md
LICENSE
.gitignore

# Project Specific
AGENTS.md
# Pi 全局指令路径：仅供仓库内子目录规则注入，不部署
.pi/agent/AGENTS.md

# Node
package.json
pnpm-lock.yaml
node_modules
tsconfig.json

# Omarchy/Arch: hypr 配置由 Omarchy Lua 模块管理，旧 Fedora hyprland.conf 不部署
{{ if and (eq .chezmoi.os "linux") (or (eq .chezmoi.osRelease.id "omarchy") (eq .chezmoi.osRelease.id "arch")) -}}
.config/hypr
{{- end }}

# Omarchy 用户插件只在 Omarchy 机器部署，其它平台不产生 ~/.config/omarchy
{{ if not (and (eq .chezmoi.os "linux") (or (eq .chezmoi.osRelease.id "omarchy") (eq .chezmoi.osRelease.id "arch"))) -}}
.config/omarchy
{{- end }}

# Platform-specific: only run the right script per platform
{{ if ne .chezmoi.os "windows" -}}
.pi/agent/merge-pi-settings.ps1*
merge-gitconfig.ps1*
{{ end -}}
{{ if eq .chezmoi.os "windows" -}}
.pi/agent/merge-pi-settings.sh*
merge-gitconfig.sh*
# 模型配置生成依赖 python3，Windows 不运行（使用仓库中已提交的生成物）
.chezmoiscripts/gen-pi-models.sh
{{ end -}}
